# Версии закреплены точно: образ должен собираться одинаково сегодня и через
# полгода. Обновление становится осознанным коммитом, а не побочным эффектом
# пересборки в другой день.
Django==6.1
celery==5.6.3
redis==8.1.0
psycopg[binary,pool]==3.3.4
gunicorn==26.1.0
